package data;
/**
 * @author hongle
 * This enum declare type of test data.
 * This framework support for both API and UI so currently there are 2 type of data (API, UI)
 */


public enum TestDataType {
    UI
    ,API
}
